// Placeholder for frontend integration test
describe('DataLab Integration', () => {
  it('should be connected to backend', () => {
    // This is a placeholder as actual E2E requires running backend
    console.log('Frontend Service configured to hit /api/datalab/export');
  });
});
